block breadcrumbs
  - if (locals.bread)
    div#breadcrumbs
      each a in bread
        - if (a.href)
          span
            a(href='#{a.href}') #{a.text}
            | >>
        - else
          span #{a.text}
